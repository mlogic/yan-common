INTRODUCTION OF REMOTE-BACKUP
=============================

Create your own config file by creating a copy of ``config.tempalte``,
which has explanation for every option.

Then run:

.. code-block:: bash

   ./remote-backup.sh my-config-file
